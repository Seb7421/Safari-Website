[install]
# Set default registry
registry = "https://registry.npmjs.org"

# Configure private registry scoped to a particular organization
#[install.scopes]
# The project no longer depends on the private GSAP registry.
